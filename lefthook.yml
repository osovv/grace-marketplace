pre-commit:
  parallel: true
  commands:
    typecheck:
      tags: typecheck
      run: bun run typecheck
    validate-marketplace:
      tags: marketplace
      run: bun run validate:marketplace

commit-msg:
  commands:
    commitlint:
      run: bunx commitlint --edit "$1"
